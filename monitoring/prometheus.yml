global:
  scrape_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "todo-app"
    static_configs:
      - targets: ["app:5000"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
